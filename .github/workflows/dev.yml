name: Push or PR to dev

on:
  push:
    branches:
      - dev

  pull_request:
    branches:
      - dev

permissions:
  contents: write

jobs: 
  quality:
    runs-on: ubuntu-latest
    environment: Testing
    steps:
      - uses: actions/checkout@v5
      - uses: ./.github/actions/linter
#        with:
#            flux_license_user: ${{ secrets.FLUX_LICENSE_USER }}
#            flux_license_pass: ${{ secrets.FLUX_LICENSE_PASS }}
#            composer_auth_pat: ${{ secrets.COMPOSER_AUTH_PAT }}

  test:
    runs-on: ubuntu-latest
    environment: Testing
    steps:
      - uses: actions/checkout@v5
      - uses: ./.github/actions/tests
#        with:
#            flux_license_user: ${{ secrets.FLUX_LICENSE_USER }}
#            flux_license_pass: ${{ secrets.FLUX_LICENSE_PASS }}
#            composer_auth_pat: ${{ secrets.COMPOSER_AUTH_PAT }}